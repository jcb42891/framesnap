<Window x:Class="FrameSnap.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        KeyDown="OnOverlayKeyDown"
        MouseMove="OnOverlayMouseMove"
        PreviewMouseLeftButtonDown="OnOverlayMouseLeftButtonDown">
    <Canvas>
        <Rectangle x:Name="InputSurface"
                   Fill="#01000000"
                   Width="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=ActualWidth}"
                   Height="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=ActualHeight}" />
        <Path x:Name="ShadeMask" Fill="#88000000" IsHitTestVisible="False" />
        <Border x:Name="CaptureRect"
                Width="640"
                Height="360"
                BorderBrush="#FFFFFFFF"
                BorderThickness="2"
                Background="Transparent"
                IsHitTestVisible="False" />
    </Canvas>
</Window>
